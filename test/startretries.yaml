programs:
  crash_test:
    cmd: "/bin/sh -c 'sleep 2 && exit 0'"
    autostart: true
    autorestart: always
    startsecs: 1
    startretries: 3
    exitcodes: [0]
    stdout: "/tmp/crash_test.out"
    stderr: "/tmp/crash_test.err"
