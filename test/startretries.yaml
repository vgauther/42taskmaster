programs:
  read_missing_file:
    cmd: "/bin/sh -c 'cat /tmp/nonexistent_file.txt'"
    autostart: true
    autorestart: never        # On ne redémarre pas en boucle infinie
    startretries: 3           # Tente 3 fois au démarrage
    startsecs: 0
    exitcodes: [0]
