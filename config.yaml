programs:
  test_fail_fast:
    cmd: "python3 -c 'import sys; sys.exit(1)'"
    autostart: true
    startsecs: 3
    startretries: 3
    autorestart: never  # pas important ici
    stdout: "./logs/prog_with_logs.out"
    stderr: "./logs/prog_with_logs.err"