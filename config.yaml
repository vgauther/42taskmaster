programs:
  createfile:
    cmd: "/bin/bash -c 'touch /tmp/testfile && ls -l /tmp/testfile'"
    stdout: "logs/createfile.out"
    stderr: "logs/createfile.err"
    umask: "077"          # Seul root/l'utilisateur pourra lire le fichier (pas les autres)
    autostart: true